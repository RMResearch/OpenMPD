using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ColourDescriptorAsset : MonoBehaviour
{
    protected Colours_Descriptor descriptor;
    public uint GetDescriptorID() { return descriptor.coloursDescriptorID; }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
